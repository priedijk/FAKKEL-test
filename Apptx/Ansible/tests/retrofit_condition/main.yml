---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

      - name: test variables
        include_tasks: condition-test.yml

      - name: Print ending
        debug:
          msg: "after ending play message"