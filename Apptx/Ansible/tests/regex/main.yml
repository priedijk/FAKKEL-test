---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

      - name: test variables
        include_tasks: regex.yml

      - name: Print ending
        debug:
          msg: "after ending play message"