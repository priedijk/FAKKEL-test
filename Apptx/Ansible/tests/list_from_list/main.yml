---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

      - name: test loop
        include_tasks: list.yml

      - name: Print ending
        debug:
          msg: "after ending play message"